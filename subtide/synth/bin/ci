#!/usr/bin/env sh

set -xe

rm -r classes || true
mkdir -p classes

clojure "-M:test:test-runner"
clojure -M -e "(do (compile 'subtide.live) (System/exit 0))"
clojure -M -e "(do (require 'subtide.live) (System/exit 0))"

rm -r classes || true
